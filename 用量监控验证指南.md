# 📊 用量监控系统验证指南

## 🎯 验证目标
确认用量监控系统是否正在显示**真实数据**还是**fallback模拟数据**

## 🔍 验证步骤

### 第一步：检查后端API状态
```bash
# 1. 检查用量记录API
curl -H "X-Ptoken: test-user" http://localhost:3000/api/usage

# 2. 检查用量统计API  
curl -H "X-Ptoken: test-user" http://localhost:3000/api/usage/stats
```

**期望结果**：
- 返回JSON格式的真实数据
- `success: true`
- 数据随API调用而变化

### 第二步：检查前端API连接
```bash
# 打开浏览器开发者工具（F12）
# 访问：http://localhost:3000/#/user/usage
# 查看网络请求标签页，是否有：
# - GET /api/usage
# - GET /api/usage/stats
```

**期望结果**：
- 看到API请求
- 请求状态码为200
- 返回真实数据

### 第三步：生成新的用量数据
```bash
# 发起一次AI服务调用
curl -X POST -H "X-Ptoken: verify-user" -H "Content-Type: application/json" \
  -d '{"prompt":"验证用量监控","options":{}}' \
  http://localhost:3000/chat-process
```

**期望结果**：
- 后端日志显示："🔍 用量追踪启动"
- 随后显示："用量记录保存成功"

### 第四步：验证数据更新
```bash
# 检查新数据是否被记录
curl -H "X-Ptoken: verify-user" http://localhost:3000/api/usage

# 刷新前端页面，检查数据是否更新
```

**期望结果**：
- API返回包含新记录的数据
- 前端页面显示更新的数据

## 🚨 问题诊断

### 情况1：显示固定的模拟数据
**症状**：
- 总费用：$2.56
- 总请求：42次
- 服务统计：OpenAI 60%, Midjourney 40%

**原因**：前端API调用失败，显示fallback数据

**解决方案**：
1. 检查浏览器开发者工具网络请求
2. 确认API代理配置正确
3. 检查认证token是否正确

### 情况2：数据不实时更新
**症状**：
- API调用后数据不变化
- 需要长时间等待才能看到更新

**原因**：批处理机制（30秒延迟）

**解决方案**：
- 等待30秒让数据写入文件
- 或者添加强制刷新功能

### 情况3：API调用失败
**症状**：
- 浏览器控制台显示网络错误
- API请求返回404或500

**原因**：
- 后端服务未启动
- 代理配置错误
- 认证失败

**解决方案**：
1. 确认后端服务运行在3002端口
2. 检查Vite代理配置
3. 使用正确的认证token

## 📋 快速验证清单

### ✅ 后端验证
- [ ] 后端服务正在运行（端口3002）
- [ ] API `/api/usage` 返回JSON数据
- [ ] API `/api/usage/stats` 返回统计数据
- [ ] 日志显示用量追踪活动

### ✅ 前端验证
- [ ] 前端服务正在运行（端口3000）
- [ ] 浏览器开发者工具显示API请求
- [ ] API请求返回200状态码
- [ ] 没有JavaScript错误

### ✅ 数据流验证
- [ ] AI服务调用触发用量记录
- [ ] 30秒内数据批处理完成
- [ ] 前端页面显示更新数据
- [ ] 统计数据正确计算

## 🔧 调试命令

### 检查服务状态
```bash
# 检查端口占用
netstat -an | grep -E "3000|3002"

# 检查进程
ps aux | grep -E "pnpm|node|esno"
```

### 检查日志
```bash
# 查看后端日志（应显示用量追踪信息）
# 日志应包含：
# - "🔍 用量追踪启动"
# - "📤 流式响应结束"
# - "用量记录保存成功"
```

### 手动测试API
```bash
# 测试认证
curl -H "X-Ptoken: $(echo -n 'test-user' | base64)" http://localhost:3000/api/usage

# 测试不同用户
curl -H "X-Ptoken: different-user" http://localhost:3000/api/usage
```

## 💡 预期正确状态

### 真实数据特征
- 费用数字为小数（如$0.04, $0.015）
- 请求时间戳为真实时间
- 用户ID为base64编码字符串
- 包含响应时间和状态码元数据

### Fallback数据特征
- 费用为整数（如$2.56）
- 固定的请求数量（42次）
- 固定的服务分布（60%/40%）
- 缺少真实的时间戳和元数据

## 🎯 验证结论

如果执行以上步骤后：
- **API返回真实变化的数据** → 系统正常工作
- **前端显示相同的真实数据** → 完全正常
- **前端显示固定数据但API正常** → 前端连接问题
- **API返回空数据但有调用日志** → 批处理延迟，等待30秒

使用此指南可以明确判断用量监控系统的实际状态！