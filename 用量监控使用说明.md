# 用量监控系统使用说明

## 📊 功能概述

用量监控系统已经完全集成到应用中，可以自动追踪所有AI服务的使用情况并提供详细的统计分析。

## 🎯 监控范围

### 已监控的AI服务
- **OpenAI** (`/openapi/*`, `/chat-process`) - GPT对话、DALL-E图像生成、Whisper语音识别、TTS
- **Midjourney** (`/mjapi/*`) - AI图像生成和编辑
- **Suno** (`/suno/*`, `/sunoapi/*`) - AI音乐生成
- **Luma** (`/luma/*`, `/pro/luma/*`) - AI视频生成
- **Viggle** (`/viggle/*`, `/pro/viggle/*`) - 视频动画生成
- **Runway** (`/runway/*`, `/runwayml/*`) - 视频编辑和生成
- **Kling** (`/kling/*`) - 视频生成
- **Ideogram** (`/ideogram/*`) - 图标和标志生成
- **Pika** (`/pika/*`) - 视频生成
- **Udio** (`/udio/*`) - 音乐生成
- **Pixverse** (`/pixverse/*`) - 像素艺术生成
- **Vidu** (`/vidu/*`, `/pro/vidu/*`) - 视频生成

## 🚀 如何使用

### 1. 访问用户中心
```
http://localhost:1002/user
```

### 2. 查看用量统计
- 点击左侧菜单的 "用量监控"
- 查看总体统计信息（调用次数、总花费、Token消耗）
- 查看各服务使用分布
- 查看今日使用概览

### 3. 筛选用量记录
- 选择日期范围进行筛选
- 按服务类型筛选
- 点击"查询"按钮应用筛选条件

### 4. 导出用量数据
- 点击"导出"按钮
- 系统会生成CSV格式的用量报告
- 包含详细的调用记录和费用信息

## 💡 监控原理

### 后端自动追踪
系统在所有AI服务的API端点上自动安装了用量追踪中间件：
```javascript
// 示例：OpenAI服务监控
app.use('/openapi', authV2, usageTracker('openai'), proxy(...))
```

### 数据存储
- 用量数据存储在 `./data/usage_records.json`
- 每次API调用都会自动记录：
  - 服务类型（openai, midjourney等）
  - 操作类型（如/chat-process, /mjapi/submit/imagine）
  - 使用的模型（如gpt-4, dall-e-3）
  - Token消耗（输入/输出Token）
  - 费用计算
  - 时间戳和请求ID

### 实时统计
- 每次访问用量监控页面时自动更新统计数据
- 支持按时间段、服务类型等维度统计
- 自动计算费用占比和使用趋势

## 🛠 开发者功能

### API端点
```bash
# 获取用量记录
GET /api/usage?service=openai&startDate=2024-01-01&endDate=2024-12-31&limit=100

# 获取用量统计
GET /api/usage/stats?period=month

# 导出用量数据
GET /api/usage/export?format=csv
```

### 自定义价格体系
价格配置位于 `service/src/utils/pricing.ts`:
```javascript
const pricingConfig = {
  openai: {
    'gpt-4': { input: 0.03, output: 0.06 }, // 每1K token价格
    'gpt-3.5-turbo': { input: 0.001, output: 0.002 }
  },
  midjourney: {
    default: { perImage: 0.04 } // 每张图片价格
  }
  // ... 其他服务配置
}
```

## 📈 使用示例

### 测试用量监控
1. **发起AI请求**：
   ```bash
   # 测试OpenAI对话
   POST http://localhost:3002/chat-process
   Headers: X-Ptoken: your-token
   Body: {"prompt": "Hello, GPT!"}
   
   # 测试Midjourney图像生成
   POST http://localhost:3002/mjapi/submit/imagine  
   Headers: X-Ptoken: your-token
   Body: {"prompt": "beautiful landscape"}
   ```

2. **查看用量记录**：
   - 访问 `http://localhost:1002/user/usage`
   - 刷新页面查看最新的使用记录
   - 检查统计数据是否更新

3. **验证数据存储**：
   ```bash
   # 查看存储的用量数据
   cat ./data/usage_records.json
   ```

## ⚠️ 注意事项

### 认证要求
所有监控的API端点都需要认证：
- 请求头必须包含 `X-Ptoken` 或 `Authorization`
- 用户ID从认证token中提取用于数据隔离

### 数据持久化
- 用量数据存储在本地JSON文件中
- 生产环境建议替换为数据库存储
- 当前实现支持高并发写入和批处理

### 性能考虑
- 用量追踪是异步进行的，不影响API响应速度
- 使用批处理机制减少磁盘I/O
- 内存中缓存统计数据提高查询性能

## 🔧 配置选项

### 环境变量
```bash
# 数据存储路径（可选）
USAGE_DATA_PATH=./data/usage_records.json

# 批处理大小（可选）
USAGE_BATCH_SIZE=10

# 统计缓存时间（秒，可选）
USAGE_CACHE_TTL=300
```

## 📊 统计维度

系统提供以下统计维度：
- **时间维度**：按日、周、月统计
- **服务维度**：各AI服务的使用情况对比
- **操作维度**：不同API操作的调用频次
- **成本维度**：费用分析和成本控制
- **用户维度**：多用户使用情况隔离

现在用量监控系统已经完全可用，您可以开始使用任何AI服务，系统会自动记录和统计所有的使用情况！